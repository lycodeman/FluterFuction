import 'package:flutter/material.dart';

class JsonSer extends StatelessWidget {
  const JsonSer({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
